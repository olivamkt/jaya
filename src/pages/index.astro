---
import { Image } from 'astro:assets'
import Layout from '@/layouts/Layout.astro'
import { getLangFromUrl, useTranslations } from '@/i18n/utils'
import LanguageSelector from '@/components/LanguageSelector.astro'
import { Atom, Languages, LucideZap, Waves } from 'lucide-react'
import '@/styles/index.css'

const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)
---

<Layout title={t('home.title')} description={t('home.description')}>
	<main>
		<section class="container">
			<h1 class="text-5xl font-bold">
				{t('home.title')}
			</h1>
			<p>
				{t('home.description')}
			</p>
			<div class="big-number-section">
				<div class="big-number">
					<div class="content">
						<div class="number-container">
							<div class="number">200</div>
						</div>
						<div class="text">Projects<br /> Delivered</div>
					</div>
				</div>
				<div class="big-number">
					<div class="content">
						<div class="number-container">
							<div class="number">200</div>
						</div>
						<div class="text">Projects<br /> Delivered</div>
					</div>
				</div>
				<div class="big-number">
					<div class="content">
						<div class="number-container">
							<div class="number">200</div>
						</div>
						<div class="text">Projects<br /> Delivered</div>
					</div>
				</div>
			</div>
			<!-- <h1 class="font-heading text-5xl font-bold">
				{t('home.title')}
			</h1>
			<h2 class="font-heading text-2xl font-medium text-pretty">
				{t('home.description')}
			</h2>
			<div class="my-10 grid grid-flow-row gap-6 md:grid-cols-2">
				<div
					class="border-opacity-40 flex w-full flex-col gap-6 rounded-2xl border-[1px] border-dashed border-black p-8"
				>
					<Languages size={40} absoluteStrokeWidth />
					<h3 class="font-heading text-xl font-medium">
						{t('home.content.1.title')}
					</h3>
					<p>
						{t('home.content.1.description')}
					</p>
				</div>
				<div
					class="border-opacity-40 flex w-full flex-col gap-6 rounded-2xl border-[1px] border-dashed border-black p-8"
				>
					<LucideZap size={40} absoluteStrokeWidth />
					<h3 class="font-heading text-xl font-medium">
						{t('home.content.2.title')}
					</h3>
					<p>
						{t('home.content.2.description')}
					</p>
				</div>
				<div
					class="border-opacity-40 flex w-full flex-col gap-6 rounded-2xl border-[1px] border-dashed border-black p-8"
				>
					<Atom size={40} absoluteStrokeWidth />
					<h3 class="font-heading text-xl font-medium">
						{t('home.content.3.title')}
					</h3>
					<p>
						{t('home.content.3.description')}
					</p>
				</div>
				<div
					class="border-opacity-40 flex w-full flex-col gap-6 rounded-2xl border-[1px] border-dashed border-black p-8"
				>
					<Waves size={40} absoluteStrokeWidth />
					<h3 class="font-heading text-xl font-medium">
						{t('home.content.4.title')}
					</h3>
					<p>
						{t('home.content.4.description')}
					</p>
				</div>
			</div>
			<div class="flex flex-col items-center justify-between gap-6 sm:flex-row">
				<a
					href="http://cokeposada.com"
					target="_blank"
					rel="noopener noreferrer"
					class="text-xs underline underline-offset-2"
				>
					{t('home.footer')}
				</a>
				<LanguageSelector />
			</div> -->
		</section>
	</main>
</Layout>
